import SectionLoading from "@/components/system/SectionLoading";

/** Sits under the cards tab bar, so crossing the section keeps its chrome. */
export default function Loading() {
  return <SectionLoading label="Loading cards…" />;
}
