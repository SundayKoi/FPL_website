import SectionLoading from "@/components/system/SectionLoading";

/** Under the betting sub-nav and balance chip, which stay put. */
export default function Loading() {
  return <SectionLoading label="Loading betting…" rows={3} />;
}
