# Broadcast Hub landing page final-fix report

Date: 2026-08-10
Branch: `feature/landing-page`

## Scope completed

Implemented the requested final-review fix wave only:

- Strengthened `LeagueHub` and `SiteNavigation` tests so `Stats`, `Schedule`, and `Info` are asserted individually and verified as noninteractive.
- Updated global smooth scrolling to respect `prefers-reduced-motion: reduce` while preserving the existing sticky-header scroll padding and theme tokens.
- Replaced the root layout metadata description boilerplate with Franchise Premier League copy and preserved the existing title.

## Changed files

- `src/app/globals.css`
- `src/app/layout.tsx`
- `src/app/layout.test.tsx`
- `src/components/SiteNavigation.test.tsx`
- `src/components/home/LeagueHub.test.tsx`

## RED/GREEN evidence

### RED

Command:

```bash
PATH="/Users/matthewwolanski/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" npx vitest run src/components/SiteNavigation.test.tsx src/components/home/LeagueHub.test.tsx src/app/layout.test.tsx
```

Result: failed as expected before the metadata change.

Relevant output:

```text
FAIL  src/app/layout.test.tsx > layout metadata > describes the Franchise Premier League landing page
AssertionError: expected 'Generated by create next app' to be 'Franchise Premier League draft hub for live broadcasts, league updates, and active drafts.'
```

Notes:

- The strengthened `SiteNavigation` and `LeagueHub` assertions already matched the existing production behavior.
- The new layout metadata assertion provided the explicit red/green coverage for the production metadata fix.

### GREEN

Command:

```bash
PATH="/Users/matthewwolanski/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" npx vitest run src/components/SiteNavigation.test.tsx src/components/home/LeagueHub.test.tsx src/app/layout.test.tsx
```

Result:

```text
Test Files  3 passed (3)
Tests  3 passed (3)
```

## Verification commands and output

### Full unit tests

Command:

```bash
PATH="/Users/matthewwolanski/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" npm test
```

Output summary:

```text
Test Files  8 passed (8)
Tests  22 passed (22)
```

### Lint

Command:

```bash
PATH="/Users/matthewwolanski/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" npm run lint
```

Output summary:

```text
/Users/matthewwolanski/Dev/FPL/FPL_website/src/app/login/page.tsx
  43:20  warning  Do not use `location.href` to navigate to internal Next.js pages. Use `redirect()` in the render phase, or `useRouter().push()` in Client Components' event handlers instead. See: https://nextjs.org/docs/messages/no-location-assign-relative-destination  @next/next/no-location-assign-relative-destination

✖ 1 problem (0 errors, 1 warning)
```

Exit code was 0.

### Webpack production build

Command:

```bash
PATH="/Users/matthewwolanski/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" npm run build -- --webpack
```

Output summary:

```text
▲ Next.js 16.3.0 (webpack)
✓ Compiled successfully
✓ Generating static pages
```

Build completed successfully with exit code 0.

## Implementation details

- `src/components/SiteNavigation.test.tsx`
  - Added named assertions for `Stats`, `Schedule`, and `Info`.
  - Verified each future destination is not exposed as a link or button.

- `src/components/home/LeagueHub.test.tsx`
  - Added named assertions for `STATS`, `SCHEDULE`, and `INFO`.
  - Verified each card remains noninteractive while `Draft` stays the only active destination in that grid.

- `src/app/layout.tsx`
  - Replaced the default description with: `Franchise Premier League draft hub for live broadcasts, league updates, and active drafts.`
  - Preserved the existing title: `FPL Draft League`.

- `src/app/layout.test.tsx`
  - Added a focused metadata test to cover the description update.

- `src/app/globals.css`
  - Kept `scroll-behavior: smooth` and `scroll-padding-top: 5rem` on `html`.
  - Added a reduced-motion override:

```css
@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}
```

## Concerns

- `npm run lint` still reports one existing warning in `src/app/login/page.tsx` about `location.href` internal navigation. This file was outside the requested fix wave and was not changed.
- Vitest emits an existing Vite config warning about `configLoader: 'native'` compatibility with `vitest.config.ts`. Tests still passed; no change was made because it is unrelated to this review scope.
